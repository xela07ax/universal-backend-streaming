# 👨‍💻 Инженерный регламент Hydro Engine

## 🌐 Сетевое взаимодействие
Система поддерживает два режима, переключаемых в `configs/*.yaml`:
- **Local Mode (`discovery.enabled: false`)**: Использует прямые IP из секции `database.host` и `redis.host`.
- **Discovery Mode (`discovery.enabled: true`)**: Динамически запрашивает адреса у Consul. Если сервис в Consul не найден, система выдает `WARN` и делает откат на локальный конфиг.

## 🔐 Безопасность и Сессии
- **JWT Secret**: Хранится в конфиге. В режиме `production` обязательно включен `auth.secure_cookie: true`.
- **Refresh Tokens**: Хранятся в Redis с ключом `session:<token>`. Срок жизни (TTL) в Redis строго синхронизирован с `refresh_token_ttl` из конфига.
- **CORS**: Разрешенные домены настраиваются через `server.cors.allowed_origins`. Флаг `allow_local` автоматически добавляет порты localhost и Vite.

## 📦 Сборка и Бинарники
- Все исполняемые файлы помещаются в папку `/bin` (игнорируется Git).
- Сборка Docker-образа использует Go 1.25.5-alpine для минимизации размера (итоговый образ ~35MB).
- В проекте используется `Makefile` для автоматизации рутинных задач. Это позволяет поддерживать единый стандарт разработки для всей команды.

## 🚀 Добавление нового функционала
1. **API**: Опишите роут в `internal/api/docs_generator.go`.
2. **БД**: Создайте SQL миграцию в `migrations/`.
3. **Frontend**: Используйте `client` из `api.js` для автоматической обработки 401 ошибок.

### 🛠 Отладка
Вы запускаете сервер на порту 8080 (согласно вашему config.yaml). Поэтому адрес в браузере должен быть:
👉 http://localhost:8080/ (или http://localhost:8080/index.html).
Пароль:hydro-super-secret-key-2026-change-me. Чтобы список не был пустым, убедитесь, что в вашей базе есть хотя бы одна запись в таблице media_assets. Можно загрузить файл через интерфейс.

#### Часто запускаемые скрипты
```bash
    docker compose -f docker-compose-pg.yml up
    make migrate-up FLAGS="--database.debug"
    go run main.go migrate --config configs/config.hybrid.yaml --database.debug
    go build -o bin/hydro .
    ./bin/hydro migrate --database.debug
    ./bin/hydro serve --server.debug --database.debug
    
     cd web
     npm run build
 ```

#### Что делать если Frontend не работает
**Собрали ли вы фронтенд?**
Ваш Go-код ищет файлы в папке ./web/dist. Если вы просто создали файлы Vue, но не «сбилдили» их, папка dist либо пуста, либо её нет.
Выполните в терминале:
```bash
cd web && npm run build
```
После этого в корне проекта должна появиться папка web/dist с файлом index.html внутри.
Обновить страницу в браузере http://localhost:8080.
**Как быстро проверить, работает ли раздача статики**:
Создайте вручную файл web/dist/index.html (если папка пуста).
Напишите там:
   ```<h1>Hydro Static Test</h1>.```
Запустите сервер: ./hydro serve.
Перейдите по адресу: http://localhost:8080/.
Если по :8080 заходит, а фронтенд Vue все равно не виден:

#### Как запустить Frontend в режиме Live:
   В одном терминале (Бэкенд): ./hydro serve --config configs/hydro.yaml
   Во втором терминале (Фронтенд): cd web && npm run dev
   Откройте: http://localhost:5173

#### Генерация пароля
Как создать хеш пароля для базы (SQL)
Чтобы ваш админ смог зайти, нужно положить в базу правильный хеш. Сгенерировать его можно командой:
```bash
go run main.go hash-password --password "ваш_пароль"
```
готовый хеш можно вставить в таблицу в поле пароля.

### Пример регистрации сервисе Redis в Consul через curl
В проде сервисы должны зарегистрироваться. Вы можете добавить в main.go или запустить разовый скрипт: Поскольку Consul запущен в режиме -dev, он «забывает» всё после перезагрузки. Вам нужно отправить HTTP-запрос на удаленный сервер (замените REMOTE_IP на адрес вашего сервера), чтобы «прописать» там вашу базу.
1. Выполните это в терминале вашей локальной машины (замените REMOTE_IP на адрес вашего сервера):
 ```bash
curl --header "X-Consul-Token: hydro-admin-token-2026" \
     --request PUT \
     --data '{
       "ID": "db-1",
       "Name": "db-service",
       "Address": "192.168.72.37",
       "Port": 5432,
       "Check": {
         "TCP": "192.168.72.37:5432",
         "Interval": "10s"
       }
     }' \
     http://192.168.72.37:8500/v1/agent/service/register
```
2. Проверка регистрации
   Откройте в браузере http://REMOTE_IP:8500.
   Войдите с токеном hydro-admin-token-2026.
   Убедитесь, что сервис db-service появился в списке и он зеленый (passing).
3. Диагностика через CLI
   Чтобы убедиться, что Consul на удаленном сервере действительно "видит" базу, выполните на локальной машине:
 ```bash
curl --header "X-Consul-Token: hydro-admin-token-2026" \
     http://REMOTE_IP:8500/v1/health/service/db-service?passing
```

#### Как тестировать "падение" Service Discovery
Чтобы проверить отказоустойчивость (то, о чем мы говорили ранее):
1. Запустите всё через docker-compose up.
2. Убедитесь, что hydro-app работает (логирует Connected to Redis).
3. Остановите консул: docker stop hydro-consul.
4. Попробуйте сделать запрос к API. Если мы настроили кэширование адресов в Discovery, приложение продолжит работать.

#### Как запустить финальную сборку docker
Когда всё синхронизировано, выполните:
```bash
docker build -t hydro-engine:latest .
```

#### порт 8080 уже занят (зомби-процесс)
При разработке в режиме make dev это обычно происходит по двум причинам:
Вы запустили make dev второй раз, не дождавшись завершения предыдущего процесса.
Прошлый запуск не завершился корректно (зомби-процесс), и порт остался заблокирован.
 ```powershell
# Найти процесс на порту 8080 и убить его
Stop-Process -Id (Get-NetTCPConnection -LocalPort 8080).OwningProcess -Force
```
```bash
fuser -k 8080/tcp
```

#### Проблемы при компиляции Frontend, метод «чистой установки»
**Полная переустановка зависимостей**:
Перейдите в папку web и удалите всё лишнее, чтобы исключить конфликт кэша:
```powershell
cd web
# Удаляем старые папки и лок-файлы
rm -r node_modules, package-lock.json
# Устанавливаем всё заново
npm install
# Явно доставляем плагин Vue (актуальная версия)
npm install @vitejs/plugin-vue@latest --save-dev
```
**Что делать, если всё равно не работает**:
Запустите команду прямой сборки $npx vite build (в папке web).